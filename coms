view.sel()[0].a
view.sel().clear()
view.sel().add(0)
row, col = view.rowcol(view.sel()[0].a)
view.text_point(row, col)
sublime.Region(0, view.size())
view.substr(sublime.Region(0, view.size()))
view.line(sel[0])
view.substr(view.line(sel[0]))
view.lines(sublime.Region(0, view.size()))
view.window().show_input_panel('Tab Size:', '', self.on_done, None, None)
view.window().new_file()
is_empty = view.sel()[0].empty()
view.word(sel)
view.show_popup('.....')
view.update_popup('abc')
view.hide_popup()
view.set_status('key', 'Loading')
view.erase_status('key')

v = view.window().new_file()
v.settings().set('auto_indent', False)
v.run_command('insert', {'characters': text})
v.settings().set('auto_indent', True)

sublime.log_commands(True)

view.show(0)
